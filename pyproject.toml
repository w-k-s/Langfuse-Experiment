[project]
name = "langfuse-experiment"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "langchain>=1.3.18",
    "langchain-ollama>=1.1.0",
    "langfuse>=4.15.1",
    "langgraph>=1.2.11",
    "python-dotenv>=1.2.3",
    "pyyaml>=6.0.3",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]

[tool.pytest.ini_options]
pythonpath = ["."] # So that tests can call 